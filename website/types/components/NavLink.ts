/**
 * Type definitions for the props of the `NavLink` component.
 *
 * @todo add examples
 *
 * @public
 */
export interface NavLinkProps {
  /**
   * href represents the hyperlink to an internal URL.
   */
  href: string;
}
